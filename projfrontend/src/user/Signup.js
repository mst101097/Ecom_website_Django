import React from "react";
import Base from "../core/Base";

const signup = () => {
    return(
        <Base title = "Signup Page" description = "A Signup for Store User">
            <p>Test of Signup page</p>
        </Base>
    )
}

export default signup;